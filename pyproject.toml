[project]
name = "adlm-knee-osteoarthritis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "black>=25.11.0",
    "dtale>=3.18.2",
    "numpy<2",
    "pandas>=2.3.3",
    "pydicom>=3.0.1",
    "torch>=2.0.0,<2.3.0; sys_platform == 'darwin' and platform_machine == 'x86_64'",
    "torch>=2.0.0; sys_platform != 'darwin' or platform_machine != 'x86_64'",
    "torchvision>=0.15.0,<0.18.0; sys_platform == 'darwin' and platform_machine == 'x86_64'",
    "torchvision>=0.24.1; sys_platform != 'darwin' or platform_machine != 'x86_64'",
    "tqdm>=4.67.1",
    "vedo>=2025.5.4",
]
